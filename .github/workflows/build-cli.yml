name: Build CLI

permissions:
  contents: read

on:
  push:
    branches: [ master, develop ]
    paths:
      - 'src/OutlookMcp.CLI/**'
      - 'src/OutlookMcp.Core/**'
      - 'src/OutlookMcp.ComInterop/**'
      - 'tests/OutlookMcp.CLI.Tests/**'
      - 'tests/OutlookMcp.Core.Tests/**'
      - 'tests/OutlookMcp.ComInterop.Tests/**'
      - 'global.json'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - '.github/workflows/build-cli.yml'
  pull_request:
    branches: [ master ]
    paths:
      - 'src/OutlookMcp.CLI/**'
      - 'src/OutlookMcp.Core/**'
      - 'src/OutlookMcp.ComInterop/**'
      - 'tests/OutlookMcp.CLI.Tests/**'
      - 'tests/OutlookMcp.Core.Tests/**'
      - 'tests/OutlookMcp.Diagnostics.Tests/**'
      - 'tests/OutlookMcp.ComInterop.Tests/**'
      - 'global.json'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - '.github/workflows/build-cli.yml'

jobs:
  build-cli:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v5

    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 9.0.x

    - name: Restore dependencies
      run: dotnet restore src/OutlookMcp.CLI/OutlookMcp.CLI.csproj

    - name: Build CLI
      run: dotnet build src/OutlookMcp.CLI/OutlookMcp.CLI.csproj --no-restore --configuration Release

    - name: Verify Core Commands Coverage
      run: |
        Write-Output "Verifying Core Commands coverage..."
        # audit-core-coverage.ps1 (regex-scraping a hand-authored ToolActions.cs) predates the
        # move to Roslyn source generators (#5/#11) -- actions are now generated directly from
        # [ServiceAction] attributes on Core interfaces, so ToolActions.cs no longer exists and
        # the script silently reported "0/0 = 100% coverage", a false-green gate (#25). Replaced
        # with the reflection-based CoreCommandsCoverageTests, which enumerates the live Outlook
        # Core interfaces/enums and genuinely fails if any method lacks an enum value.
        dotnet test tests/OutlookMcp.McpServer.Tests --filter "FullyQualifiedName~CoreCommandsCoverageTests" --configuration Release
        if ($LASTEXITCODE -ne 0) {
          Write-Error "Coverage gaps detected! All Core methods must be exposed via MCP Server."
          exit 1
        }
        Write-Output "Coverage audit passed - all Core methods have matching enum actions"
      shell: pwsh

    - name: Verify CLI build
      run: |
        # Check outlookcli main executable
        if (Test-Path "src/OutlookMcp.CLI/bin/Release/net9.0-windows/outlookcli.exe") {
          Write-Output "✅ outlookcli.exe built successfully"
          $version = (Get-Item "src/OutlookMcp.CLI/bin/Release/net9.0-windows/outlookcli.exe").VersionInfo.FileVersion
          Write-Output "Version: $version"

          # Test CLI help command (safe - no PowerPoint COM required)
          $helpOutput = & "src/OutlookMcp.CLI/bin/Release/net9.0-windows/outlookcli.exe" --help 2>&1
          $exitCode = $LASTEXITCODE

          if ($exitCode -eq 0) {
            Write-Output "✅ CLI help command working (exit code: 0)"
            Write-Output "📋 Help output preview:"
            $helpOutput | Select-Object -First 5 | ForEach-Object { Write-Output "  $_" }
          } else {
            Write-Error "❌ CLI help command failed with exit code: $exitCode"
            Write-Output "Full output:"
            $helpOutput | ForEach-Object { Write-Output "  $_" }
            exit 1
          }
        } else {
          Write-Error "❌ outlookcli.exe not found"
          exit 1
        }

        Write-Output "🔧 CLI tool ready for direct automation"
      shell: pwsh

    - name: Test CLI (requires PowerPoint)
      run: |
        Write-Output "ℹ️ Note: CLI tests skipped in CI - they require Microsoft PowerPoint"
        Write-Output "   Run 'dotnet test tests/OutlookMcp.CLI.Tests/' locally with PowerPoint installed"
      shell: pwsh

    - name: Upload CLI build artifacts
      uses: actions/upload-artifact@v6
      with:
        name: OutlookMcp-CLI-${{ github.sha }}
        path: src/OutlookMcp.CLI/bin/Release/net9.0-windows/

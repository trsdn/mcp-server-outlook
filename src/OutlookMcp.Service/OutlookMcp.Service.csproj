<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0-windows</TargetFramework>

    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <!-- Assembly and Namespace Configuration -->
    <AssemblyName>OutlookMcp.Service</AssemblyName>
    <RootNamespace>OutlookMcp.Service</RootNamespace>

    <!-- Service library doesn't need XML documentation warnings -->
    <NoWarn>$(NoWarn);CS1591</NoWarn>

    <!-- Version Information -->
    <Version>1.0.0</Version>
    <AssemblyVersion>1.0.0.0</AssemblyVersion>
    <FileVersion>1.0.0.0</FileVersion>

    <!-- Not a standalone package - bundled with CLI and MCP Server -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!-- Release Build Optimizations -->
  <PropertyGroup Condition="'$(Configuration)' == 'Release'">
    <DebugType>none</DebugType>
    <DebugSymbols>false</DebugSymbols>
    <Optimize>true</Optimize>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\OutlookMcp.Core\OutlookMcp.Core.csproj" />
    <ProjectReference Include="..\OutlookMcp.ComInterop\OutlookMcp.ComInterop.csproj" />
  </ItemGroup>

  <!-- Allow CLI and MCP Server tests to access internals -->
  <ItemGroup>
    <InternalsVisibleTo Include="OutlookMcp.CLI" />
    <InternalsVisibleTo Include="OutlookMcp.McpServer" />
    <InternalsVisibleTo Include="OutlookMcp.CLI.Tests" />
    <InternalsVisibleTo Include="OutlookMcp.McpServer.Tests" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="StreamJsonRpc" />
  </ItemGroup>

</Project>
